# Full URL of the middleware's /chat endpoint. Cross-origin in dev — the
# middleware sends CORS headers permitting this SPA's origin (see
# middleware/.env CORS_ORIGIN).
VITE_AGENT_URL=http://localhost:7890/chat
